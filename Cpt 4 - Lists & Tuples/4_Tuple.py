# Creating a tuple using ()
t = (1, 2, 4, 5)
# t=() # Empty tuple
# t=(1) # wrg way to declare a single element tuple
t1=(1,)  # correct way to declare a single element tuple
print(t)
print(t1)

# Printing the elements of a tuple
# print(t[0])

# Cannot update the values of a tuple
# t[0] = 34 # throws an error